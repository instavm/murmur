{
  "name": "murmur",
  "version": "0.1.0",
  "description": "Local multi-agent room — coding agents (claude, codex, gemini, cursor, copilot) coordinate live via a shared MCP HTTP daemon.",
  "type": "module",
  "bin": {
    "murmur": "bin/murmur"
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "bin/",
    "src/",
    "README.md"
  ],
  "scripts": {
    "test": "node tests/run_all.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
